{
  "info": {
    "_postman_id": "c0a7f3d2-2b9a-4e1f-9a3e-7c1d4b6e8f10",
    "name": "Chowly POS Integration API",
    "description": "Partial, best-effort Postman collection for the Chowly POS integration API. Chowly's API is partner-gated and provisioned per restaurant location via an API key. The requests below model the operations surfaced by Chowly's public API client libraries (get menu, create order, get order) against https://api.chowlyinc.com. Paths and payloads are representative and not authoritative; confirm with Chowly partner documentation.",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "auth": {
    "type": "apikey",
    "apikey": [
      { "key": "key", "value": "Authorization", "type": "string" },
      { "key": "value", "value": "{{apiKey}}", "type": "string" },
      { "key": "in", "value": "header", "type": "string" }
    ]
  },
  "variable": [
    { "key": "baseUrl", "value": "https://api.chowlyinc.com", "type": "string" },
    { "key": "apiKey", "value": "", "type": "string" }
  ],
  "item": [
    {
      "name": "Orders",
      "description": {
        "content": "Create and retrieve orders injected into the restaurant POS.",
        "type": "text/plain"
      },
      "item": [
        {
          "name": "Create an order",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Content-Type", "value": "application/json" }
            ],
            "url": {
              "raw": "{{baseUrl}}/orders",
              "host": ["{{baseUrl}}"],
              "path": ["orders"]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": { "raw": { "language": "json" } }
            },
            "description": "Submit an order to be injected into the restaurant POS. Path and payload are representative; the order schema is partner-gated and not publicly documented."
          },
          "response": []
        },
        {
          "name": "Retrieve an order",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/orders/:orderId",
              "host": ["{{baseUrl}}"],
              "path": ["orders", ":orderId"],
              "variable": [
                { "key": "orderId", "value": "", "description": "Identifier of the order to retrieve." }
              ]
            },
            "description": "Retrieve a previously submitted order by its identifier. Path and response schema are representative and partner-gated."
          },
          "response": []
        }
      ]
    },
    {
      "name": "Menu",
      "description": {
        "content": "Retrieve the menu synchronized across ordering channels.",
        "type": "text/plain"
      },
      "item": [
        {
          "name": "Retrieve the menu",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/menu",
              "host": ["{{baseUrl}}"],
              "path": ["menu"]
            },
            "description": "Retrieve the menu associated with the API key's location. Path and response schema are representative and partner-gated."
          },
          "response": []
        }
      ]
    }
  ]
}
